# Android build output, all generated
build/
app/build/
.gradle/

# Local machine settings, specific to one computer
local.properties
*.local.properties

# IDE files, not needed for the code itself
.idea/
*.iml
*.iws
*.ipr

# Model files, far too large for GitHub at 1-2 GB each
*.bin
*.tflite
*.task

# OS junk
.DS_Store
Thumbs.db


# Signing keys. Secret, never commit
*.jks
*.keystore
keystore.properties
