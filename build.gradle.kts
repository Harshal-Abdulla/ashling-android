// Top-level build file — think of this as the "main config" for the whole project.
// Each module (like "app") has its own build.gradle.kts for module-specific settings.
plugins {
    id("com.android.application") version "8.13.2" apply false
    id("org.jetbrains.kotlin.android") version "2.0.21" apply false
}
